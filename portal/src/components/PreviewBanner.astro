---
import { isPreview } from '../lib/env';
---

{
  isPreview() && (
    <div class="bg-[#8A5A2A] px-4 py-2 text-center text-xs font-medium tracking-wide text-cream">
      Preview employee portal — this is not the live wovensage.com website, and it is not yet
      portal.wovensage.com production.
    </div>
  )
}
